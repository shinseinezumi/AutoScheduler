zipを展開した時点などでは必要なライブラリなどがインストールされていません。
以下の手順に従ってください。

１．AutoScheduelrフォルダの直下（.envsampleや.gitignoreがある場所）にて下記のコマンドを実行してください。
npm install

２．.envsampleとまったく同じ内容の.envファイルを作成してください。

３．AutoScheduler/api下にあるreadme.txtに従って、ＡＰＩサーバーの環境を整えてください。